# agents/ MANIFEST — canonical definitions + recorded deployments

**Canonicality (ADR-0022 A5):** the definitions in THIS directory are canonical; every deployed
copy is a recorded deployment stamped here with a content hash. Drift = diffable, never
discoverable-by-accident. **Registration timing, corrected 2026-08-07
(P-20260807-pin-registration-turn-boundary):** a roster change is not live in the turn it is
written, but it is not a silent no-op either — the harness announces it with an
`agent_listing_delta` on the next user-turn boundary, and it spawns from there. Restart stays the
dependable path (n=2, one build), so still stamp the deploy AND note the restart; treat a
same-session spawn of a just-deployed pin as usable but unwarranted.

**Deploy step (until a sync script exists, documented-manual):** copy changed definitions to the
target, re-hash (`shasum -a 256 agents/*.md`), update the deployments table, restart the session.
`--check` equivalent: re-hash the target and diff against this table.

## Canonical candidates (0.1.0-stage1-draft, hashed 2026-07-10)

| definition | model × effort | sha256 | provenance |
|---|---|---|---|
| reviewer.md | **opus × high** (operator re-route 2026-07-24: "opus 5 is now the model… you can get away with opus high for reviews"; fable retained for orchestration/decomposition/brainstorming/driving-long-horizon-work (operator clarification 2026-07-24) classes. Supersedes the ADR-0022 D3 scheduled flip — executed early on a quality ruling, not the availability cliff. Registers at next session start) | c567e4057774c3d42c79b24d3e0e1decf3721f2c98235c621d08f68d3eee1593 (**multi-lens + review-scope amendment 2026-08-07**, operator: a reviewer may carry several assigned lenses, applied per-pass and attributed per finding — the former "exactly ONE lens" clause contradicted the `multi-lens-review` skill, whose default is one agent carrying several. Same pass, same operator: "judge; you do not redesign" replaced — a design review that cannot name a remedy is not a review. The rule now bars taking the artifact over and judging it against the reviewer's preferred design, not naming remedies. Applies to all three reviewer pins; DELIVERY clause added 2026-07-28; prior shas 6b8f6060…, 03b5ca02…, 2369d9d3…) | flip applied 2026-07-24 (fable leg, campaign session, operator in-session ruling); prior: fable × high per ADR-0022 A3, sha a9b8d343… |
| explorer.md | **sonnet × high**, read-only — **fallback surface since 2026-08-14**: R7 exploration/reading legs route first to gemini flash 3.7 high via `agy` (operator ruling; ROUTE-HISTORY 2026-08-14) | e473c81d48713bfc6da8416eb7d83a7ace0d80d5c8a8adc5313e50484f83ecb2 <!-- ROUTING note 2026-08-14 (flash-3.7 ruling), description-only edit, deployed same pass preview-class (installer refused: tree dirty with another session's changes); was e34e76c0…; DELIVERY clause 2026-07-31; was 535c022b… --> | carries the 2026-07-10 operator correction (facts + follow-ups, never judgments) **+ the 2026-07-17 sonnet-first ruling [W-023]**. **Canonicality repair 2026-07-24 (review D-1):** the 07-17 ruling reached ROUTES R7, W-023, and the DEPLOYED pin but never this file, which stayed `model: opus` and was stamped `abf2e469…` — so canonical contradicted the route row citing it, and `install.py claude-code` would have silently reverted the ruling in the live roster. Repaired by promoting the deployed frontmatter to canonical (bodies were byte-identical; only `description` + `model:` differed). Root cause: commit `2e3b059` changed the route + warrant without the coupled pin edit — the failure mode CLAUDE.md's profile↔pin rule exists to prevent |
| explorer-light.md | sonnet × medium, read-only — **fallback surface since 2026-08-14**: R6 sweeps route first to gemini flash 3.7 high via `agy` (operator ruling; ROUTE-HISTORY 2026-08-14) | 9a2a1cb542c240371b6c4420603b569268de1fcc1559adc33992e0147304b92e <!-- ROUTING note 2026-08-14 (flash-3.7 ruling), description-only edit, deployed same pass preview-class; was a3831963…; DELIVERY clause 2026-07-31; was 4389ba1a… --> | minted 2026-07-05 as the sonnet-medium probe instrument (W-006) |
| implementer.md | opus × **medium** (re-pointed 2026-07-24, operator ruling; Provisional — effort-frontier probe P-20260724 open, now medium-incumbent vs xhigh-challenger) | ae60398f86f73461a9fe8660a914ceb98aac87b75cb72047eac62e0c85d05a52 | W-004, W-024; ruling record P-20260724-r4-r5-reroute-opus5-effort |
| implementer-high.md | opus × **high** (R4 escalation) | c809a6e7ccd8451ae7adac1554628475fcc5475c55c84bb0b52a7b882d9531fe | minted 2026-08-07 on operator request during the DelegateOps handoff review, because R4's "high per stated reason" escalation had **no pin that could deliver it** — generic spawns inherit session effort, so the route row named a config the roster could not reach. Body = `implementer.md`'s five rules verbatim; the delta is effort, not scope. Deployed to `~/.claude/agents/` first and promoted to canonical in the same pass that recorded P-20260807-pin-registration-turn-boundary. **Capability profile UNPROBED** — no paired evidence that high beats medium on this class; the effort-frontier probe under W-024(c) is the instrument that would settle it |
| implementer-light.md | opus × **low** (re-pointed 2026-07-24, same ruling; Provisional) | 164a1da571d8f072e0d397a68f2d1dea19f2aa96f58f4f08e9a20645e5938b1a | W-005, W-024; sonnet demotion probe re-scoped (sonnet high vs opus low, count 1/3) |
| orchestrator.md | fable × high | 39bcd0c45edb246dd75cd9baa32614ff4aaeceaed300bc654d6c7a55161aff14 | minted 2026-07-10 after the effort-inheritance firing; post-window pin adjudication OWED (window premise superseded 2026-07-24 by fable-permanent — adjudication still open as a quality question) (STATE.md) |
| advisor.md | fable × xhigh, tool-less | 6f1811a5b7f46a36444d66db7da2d0e12e15a216734f6ce8a94ff20485598250 | adopted 2026-07-10 from the Cowork `delegation-roster` fork 0.2.2 (its advisor.md sha `3d3eae34…76e766`), operator-claimed design; route R15 (CANDIDATE, W-022). Basis: subagent advisor = plaintext on any model (tool caveats out of scope); xhigh reason: single bounded turn, judgment-dense checkpoint. Disconfirmer: R15's owed local probe (incl. high-vs-xhigh pair) grading advice quality/actionability — parity at high demotes the pin. Window management is project-side (graceful degradation, operator 2026-07-10): no dates in the definition |
| reviewer-xhigh.md | opus × **xhigh** (R1 escalation) | 2442f75be6e6495a29c3f6a4537c5f78f1fc3f9832dcc5d4d6af3ba8f92d6a62 <!-- multi-lens + review-scope amendments 2026-08-07; interim fdbd453a…; mint sha 2858ded9… --> | minted 2026-08-07 on operator request during the Codextend proposal-gate question, because R1's "xhigh per stated reason" escalation had **no pin that could deliver it** — generic spawns inherit session effort, and `reviewer-max`'s `max` tier is unattested. Body = `reviewer.md` plus `reviewer-max.md`'s spend-the-tier rule; the delta is effort, not scope. Deployed to `~/.claude/agents/` in the same pass (preview-class: source tree dirty). **Capability profile UNPROBED** — W-001 holds that independence outperforms depth on review gates, so a paired `reviewer` / `reviewer-xhigh` run on one artifact is the instrument that would settle whether this pin earns its place; parity demotes it. Description deliberately carries no mint rationale (operator instruction 2026-08-07: selection guidance only) |
| reviewer-max.md | opus × **max** (`max` in frontmatter UNVALIDATED — no served-effort attestation surface known; P-20260805-effort-surface-and-pin-registration finding 4) | f8d5f150c07de6626f734f2ec5fb76624b9ebbcf9aeb5f91ce48c35c0e17808d <!-- multi-lens + review-scope amendments 2026-08-07; mint sha 8785a6f2… --> | minted 2026-08-05 on operator request (stylewright measurement-review session): a one-call spawn for owner-gate reviews that tonight needed a Workflow `agent()` call to deliver {opus, max}. Deployed to `~/.claude/agents/` same pass; **registers at next session start — re-verified this date** by a failed mid-session noop spawn (same probe, finding 2). Validation owed at next start: does the spawn resolve, and does any surface attest the served tier |

Excluded from the roster migration: `knowledge-store.md` (a reference specification that was
sitting in the deployment directory, not an agent definition — left where it lives; flagged for
the operator to relocate).

## Claude package-manifest stamp contract

Release-class `install.py claude-code` runs only from a clean canonical source tree and emits a
deterministic `delegate-triage-package-manifest.json` at the Claude installation root. Before
that deployment is accepted, append its exact printed stamp to the applicable deployment row or
record using this machine-readable form:

```text
<!-- claude-package-manifest:v1 sha256=<64 lowercase hex> source_commit=<40 lowercase hex> -->
```

The stamp is an external trust anchor: `check_wids.py --scope deployment` and
`install.py claude-code --check` reject a missing or mismatched stamp. Record a real value only
after a separately authorized deployment. Do not pre-stamp a proposed manifest, reuse a stamp for
different bytes, or infer acceptance from the installed manifest alone.

## Recorded deployments

| target | stamped | status |
|---|---|---|
| `dionysus:~/.claude/` (manifest-bound 68-file package — FIRST deploy on this host) | **2026-08-14 @ `0fb42c6` (fresh clone, operator-directed tailnet deployment)** | Manifest SHA-256 `bc4dcd080e8623a520203dc4e2957fbcc63d7c2d32ca5a741c366ac1778f3d20`; <!-- claude-package-manifest:v1 sha256=bc4dcd080e8623a520203dc4e2957fbcc63d7c2d32ca5a741c366ac1778f3d20 source_commit=0fb42c6430872322e6a7daa8e893d37b641bcff7 --> Package bytes identical to the apollo deploy (the sha differs only because the stamp embeds source_commit). Suites on dionysus before deploy: adapter 25 OK · capture 5 OK · policy 26 OK · intent-writer 208 OK after the portability fix it exposed (delegate-runtime `a794b38` — bare import rode invocation-dependent sys.path). Post-install `--check`: `ok 68 · behind 0 · drift? 0 · diverged 0 · missing 0 · extra 2` (pre-existing dionysus-local pins `adversarial-auditor-xhigh`, `trajectory-verifier` — not package-owned, left in place). No `agy` binary on this host — R6/R7 legs there run the no-agy fallback pins. Unattended run, session `5f55ceae` (operator drop-in directive; charter row 5). |
| `~/.claude/` (apollo — manifest-bound 68-file package) | **2026-08-14 @ `ee18940` (release-class: clean source, third stamp this day — agy-adapter surface encoding: CONTRACT §3 adapter row, STATE `agy-adapter-invocation`, ROUTES R6 sanctioned-invocation pointer, LANES substrate lane)** | Manifest SHA-256 `22bf6a6bc91a4bd5d161e8d635de04ab8f507cc9702c0cd24ff86676ca053eb6`; <!-- claude-package-manifest:v1 sha256=22bf6a6bc91a4bd5d161e8d635de04ab8f507cc9702c0cd24ff86676ca053eb6 source_commit=ee18940cb496f61c4a2cee71ef82490321f16f34 --> Full gate green before deploy: suite 26 OK, `check_state`/`check_wids`/`check_rename` all exit 0 (individually exit-code-verified, no pipes). Post-install `--check`: `ok 68 · behind 0 · drift? 0 · diverged 0 · missing 0 · extra 8` (same ratified external overlay; overall FAIL exit is the standing extras semantics, owned files clean). No pin frontmatter changed — content-only refresh, current session reads deployed copies live. Unattended run, session `5f55ceae` (charter row 5). Supersedes the row below. |
| `~/.claude/` (manifest-bound 68-file package, superseded) | **2026-08-14 @ `82f430b` (release-class: clean source; re-stamped same run — interim `467e11f` release superseded ~2h later by the STATE `fable-usage-pacing` row sync)** | Manifest SHA-256 `87811064b1e19debf791e35971f776219a970f54e0a640ae2b9043903eafd737`; <!-- claude-package-manifest:v1 sha256=87811064b1e19debf791e35971f776219a970f54e0a640ae2b9043903eafd737 source_commit=82f430bb8d1197b72aa59521924ae343ce6cdd35 --> The owed clean re-stamp after the family-rename hand-sync (row below): full suite green (26 OK — the xhigh-override WIP adopted and completed, 65→68 acknowledged), `check_rename` + `check_task_class_enum` CLEAN. Package grew 65→68: +2 rename-day probe records via the records glob, +`P-20260724-storage-advisor-b11` **adopted from a deployed-only copy found during this pass** (write-path gap; W-024 datum was invisible to the repo). The 2026-08-07 row's standing `--check` gap is retired: b11 duplicate removed after byte-verify, `.DS_Store` removed, the two `references/` archive stubs exempted in both checker scopes (legitimate deployed-only provenance residents). Post-install `install.py claude-code --check`: `ok 68 · behind 0 · drift? 0 · diverged 0 · missing 0 · extra 8` (ratified external overlay). **Restart pending**; no pin frontmatter changed. Unattended run, session `5f55ceae` (charter row 5). Supersedes the hand-sync row below as the current deployment. |
| `~/.claude/` (family-rename hand-sync, superseded by the `467e11f` release above) | **2026-08-14, preview-class** (source tree carries another session's uncommitted changes; commit id in the migration record) | The 2026-08-14 family rename ([migration record](../docs/reviews/2026-08-14-family-naming-migration-record.md)): skill home MOVED to `skills/delegate-triage/` from the old act-name path, with a fail-loud alias stub left at that old path (one review cycle); changed stamped files hand-synced from source (`SKILL.md`, `CONTRACT.md`, `WARRANTS.md`, `check_wids.py`, root `delegation.md`, `references/routing-table.md` pointer edit); package manifest regenerated **under its new name** `delegate-triage-package-manifest.json` from actual deployed bytes with `dirty_source: true` — an honest receipt of the hand-sync, not a forged release; next clean `install.py claude-code` re-stamps. `check_wids.py --scope deployment`: 60/60 verified, 11 EXTRA = the ratified external overlay (pre-existing). `flash-executor-high` + `flash-implementer` re-hashed in the WARRANTS gateway-pin register (path citations follow the repo rename). No pin frontmatter changed; the renamed skill act-name registered mid-session. |
| `~/.claude/` (targeted file sync, not a package materialization) | **2026-08-07 @ `eb4f156` (release-class: clean source for the synced files)** | Six files copied, nothing removed and no quarantine run: skill-home `STATE.md` + `ROUTES.md` + `probes/INDEX.md` + the two pin-registration probe records, and root `delegation.md`. Reason for the targeted form rather than `install.py claude-code`: the deployed `STATE.md` is what the spawn-triage guard reads, so a canonical-only edit would have left the live surface stale, while a full materialization is a broader authorized act than this correction needs. `install.py claude-code --check` after the sync: **`ok 64 · behind 0 · drift? 0 · diverged 0 · missing 0 · extra 8`** (the 8 EXTRA are the ratified external overlay). `implementer-high.md` needed no copy — it was deployed first and promoted to canonical from those bytes. **Restart pending**; the pin is nonetheless live in the minting session per `platform-pin-registration`. Standing gap, not introduced here: `--check` still exits FAIL on four unlisted package files (a `.DS_Store`, a stale probe copy at `probes/`, and the two `references/` archive stubs). |
| `~/.claude/` (manifest-bound 60-file package) | **2026-08-04 @ `aaa7f73` (release-class: clean source)** | Manifest SHA-256 `a17d83ec50c30f495d23eb1f5495283a727ef440536a636ebf020ecad3308494`; <!-- claude-package-manifest:v1 sha256=a17d83ec50c30f495d23eb1f5495283a727ef440536a636ebf020ecad3308494 source_commit=aaa7f733bc2971a51496252e793f032153cb9317 --> package owns 60 files across the skill, seven roster definitions, and root delegation surface. The 22 byte-matched excluded fixture/runtime files were moved to recoverable quarantine; eight ratified external-overlay agents remain `EXTRA` and untouched. See the [deployment and cleanup record](../docs/reviews/2026-08-04-deployment-aware-package-integrity-deployment-cleanup-record.md). **Restart pending**; no roster definition bytes changed. Supersedes the `27527f1` package deployment below. |
| `~/.claude/` (agents + skill home + delegation.md) | **2026-07-24 @ `27527f1` (release-class: clean tree)** | 69/69 files `ok` (behind 0 · drift? 0 · diverged 0 · missing 0 · extra 3 = the RATIFIED external sol-* overlay). **First deploy carrying `~/.claude/delegation.md` as a managed target** (canonical: `adapters/claude-code/delegation.md` — the no-route-values rewrite + the two 07-24 verification rules); also carries the ratification-cascade surfaces. Restart not required for this delta (no pin frontmatter changed since `65773b1`; delegation.md is read per-conversation, not registered at session start). Supersedes the `65773b1` stamp below. |
| `~/.claude/` (superseded by `27527f1` above) | **2026-07-24 @ `65773b1` (release-class: clean tree)** | 66/66 files `ok` (`install.py claude-code --check`: behind 0 · drift? 0 · diverged 0 · missing 0 · **extra 3** = the unowned `sol-*` trio, D-2 open). Carries the same-day R1 (reviewer→opus high) and R4/R5 (implementer→opus **medium**, implementer-light→opus **low**) re-routes; roster hashes = canonical table above. **Restart required and NOT yet performed at stamp time** — the stamping session continues on the old registered pins and delivers new-route spawns per-call via Workflow `agent()` (CONTRACT §3). Supersedes the same-day preview deploy below. |
| `~/.claude/agents/` (superseded by the `65773b1` release deploy above) | **2026-07-24 @ `f467634` + UNCOMMITTED worktree (review D-2 repair deploy)** | 64/64 files current (`ok 64 · behind 0 · diverged 0 · missing 0`); roster hashes = the canonical table above. **Restart required and NOT yet performed at stamp time** — roster registers at session START. ⚠️ **Dirty-source deploy:** the source tree carried uncommitted changes, so this is a preview-class deployment, not a release; its bytes are not reconstructable from `f467634` alone. **What it repaired:** the deployed `ROUTES.md` and `STATE.md` were `DIVERGED` — bytes that existed in **no commit in repository history** (hand-edited: an R1 phrasing never committed, the `mediumw` typo, a pre-ruling R7, and a `STATE.md` stripped of its format-contract comment and `<!--vf:-->` sync tags). Adjudicated before overwrite: both were strictly **lossier** than canonical, carrying nothing canonical lacked. Pre-overwrite copies preserved outside the repo. |
| `~/.claude/agents/` — **EXTERNAL-OVERLAY REGISTER EXTENDED (2026-07-31, operator-ratified pilot-closure A′)** — the D-2 pattern now also covers `flash-executor-high.md`, `flash-implementer.md`, `sol-orchestrator.md`, `luna-explorer.md`, `terra-explorer.md` (post-amendment hashes in WARRANTS KNOWN-REPOS gateway-pin register); `flash-executor-medium.md` **QUARANTINED** to `~/.claude/agents-quarantine/` (adverse census evidence; release = one clean medium probe). The 2026-07-28 mint-without-provenance incident is recorded at [`docs/reviews/2026-07-31-pilot-closure-panel-adjudication.md`](../docs/reviews/2026-07-31-pilot-closure-panel-adjudication.md) + signal `obs-20260731T193736-2ccc2f`. Quarantine takes effect at next session start (pins register at START) | 2026-07-31 | supplements, does not supersede, the D-2 row below |
| `~/.claude/agents/` — **DECLARED EXTERNAL OVERLAY (reclassified 2026-07-24; was "UNOWNED, decision owed")** — panel-adjudicated keep-external ([`docs/reviews/2026-07-24-decision-panel-adjudication.md`](../docs/reviews/2026-07-24-decision-panel-adjudication.md) D-2: sol CONCUR_WITH_CHANGES w/ conflict disclosed, opus CONCUR; **RATIFIED by operator 2026-07-24**). Conditions attached: R1 cites immutable instrument identity (contract hash + availability predicate), not the mutable agent name; contract hashes recorded on future sol probes | discovered 2026-07-24 | `sol-advisor.md`, `sol-code-reviewer.md`, `sol-design-reviewer.md` are live in the deployment, **never tracked in this repo**, absent from the canonical table above — while `ROUTES.md` **R1 routes to two of them** and three `P-20260717-sol-*` probe records turn on their behaviour. `install.py --check` was structurally blind to them until 2026-07-24 (it inspects only files it would itself write); it now reports them as `EXTRA`. **Operator decision owed:** adopt into the canonical roster, or keep external and drop the R1 citation. Adoption is not automatic — their `model: sol` pins resolve only through a private CLIProxyAPI gateway (`claudex`), so shipping them from a public MIT package is a packaging decision, not a filing one. |
| `~/.claude/agents/` (superseded) | 2026-07-10 @ `0fa9ee8` (install.py claude-code — first scripted deploy) | roster: 7 definitions incl. `advisor` (new; hashes = canonical table above); skill home `~/.claude/skills/delegate-triage/` current @ `0fa9ee8` (31 files, `install.py claude-code --check` clean); **restart pending at stamp time** — advisor registers at next session START. History: Stage-2 deploy from `14c3311` same day (routing-table archived, guard re-pointed to STATE.md); ROUTES+WARRANTS interim re-sync @ `cf95ea1` |
| SEAS `harness/agents/` | 2026-07-10 | superseded records (marked in-file per ADR-0010 convention); TRIAGE.md re-scoped to consumer-side pointer + overlay |
| Cowork `delegation-roster` plugin (installed) | — | **UNRECONCILED FORK, discovered 2026-07-10:** a GPT-5.6-Pro repackaging of the PRE-package routing table + probe corpus (its routing-evidence.md cites the same bridgewright/prix-guesser/signal-layer records) — 7 agents (ours + an `advisor` @ fable/**xhigh**) + a skill with a routes/evidence split but **NO STATE layer** (volatile facts deliberately omitted → no expiry mechanism; its fable pins will silently outlive the 07-12 window — the C-012 failure mode in a third home). Stage-2 reconciliation owed: regenerate from canonical or diff-and-stamp; until then it is a live drifting copy in exactly the environment (Cowork) the proposal diagnosed as blind. |

## Recorded deployments

| date | artifact | sha256 | source commit | notes |
|---|---|---|---|---|
| 2026-08-07 | dist/delegation-roster-0.6.0.plugin (Cowork) | d7a2afaeda1d0038c51b92bd41d91009995a7bede4888fb1788f49f142c830e7 | 07014b9 | supersedes 0.5.0, which had drifted three ways: the installed plugin still carried the pre-amendment `reviewer` (one-lens, judge-not-redesign) and lacked both pins minted 2026-08-07 (`implementer-high`, `reviewer-xhigh`). Now 10 agents; references carry CONTRACT §2a (operator declaration outranks overlay/profile/ROUTES) and the R1 xhigh surface note. Every packed file verified byte-identical to its HEAD version at build time; `--check` clean. INSTALL THIS ONE — **INSTALL PENDING** (Cowork plugin UI; sessions pick it up after install) |
| 2026-07-25 | dist/delegation-roster-0.5.0.plugin (Cowork) | b8d357cd85719755c95c7bbdcf380669fd2e0ecfb964e6fd15e38029471f763b | 63e18c7 | supersedes 0.4.1: ROUTES slimmed per progressive disclosure (provenance -> ROUTE-HISTORY.md, canonical-only; ~580 tokens saved per triage load, checker-verified routing-identical) + probe run-1 record. INSTALL THIS ONE |
| 2026-07-25 | dist/delegation-roster-0.4.1.plugin (Cowork) | 2f53371c1b87a0a2f23eaf0ffd0249e9bef26a2b1dcae37a3ac364ee197ed020 | de6115d | supersedes same-night 0.4.0: adds batch A–D tier rulings (xhigh drops, R14→R15 merge, profile deletion, cap-4 constraint) + W-025 + R13 scope CANDIDATE. INSTALL THIS ONE |
| 2026-07-25 | dist/delegation-roster-0.4.0.plugin (Cowork) | db3801f20394f55d59c38722e773c14369b49ee71f30bff97242510fab59a116 | a5d7eaa | rebuilt post R4/R5 re-route (implementer opus medium, implementer-light opus low) + post-opus5 mechanical pass (issues #2,4,6,10,12 in docs/reviews/2026-07-24-post-opus5-routing-issues.md); the packaged ROUTES' one remaining `opus-4.8` string is the R14 provenance note (intentional). Delivered in-conversation + a copy in the operator's Development dir (env-specific) — supersedes the same-day 0.3.0 rebuild; INSTALL PENDING (registers at next session start) |
| 2026-07-24 | dist/delegation-roster-0.3.0.plugin (Cowork) | e21a552c19828b4f7b8d2c66414668e31815456da4c1c06f383f2c866d6d5798 | 29c4461 | rebuilt post R1-reroute + fable-permanent; carries the 15-row ROUTES incl. R14/R15; stale `opus-4-8` references gone (unpack-grep, hyphenated spelling only — R14's `opus-4.8` survived; corrected in ROUTES 2026-07-25 pass). Delivered to operator for Cowork-UI install — INSTALL PENDING, sessions pick it up after install |
